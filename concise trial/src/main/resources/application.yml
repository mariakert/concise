server:
   port: 8888
spring:
   datasource:
      url: jdbc:postgresql://localhost:5432/postgres
      username: postgres
      password: postgres
   jpa:
      hibernate:
         ddl-auto: validate

flyway:
   locations: classpath:/db/migration/